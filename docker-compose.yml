services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: durable
      POSTGRES_USER: durable
      POSTGRES_PASSWORD: durable
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
